/* Object.values() iterates over all values for a given object and returns an array */

const someObject = {
    a: "Value 1", 
    b: "Value 2", 
    c: "Value 3", 
    d: "Value 4"
}

valueArray = Object.values(someObject); 
console.log(valueArray);